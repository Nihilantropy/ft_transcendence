# Authorization Security - Visual Guide

**Quick Reference:** User authorization flow for profile updates

---

## 🔐 **Security Flow Diagram**

```
┌─────────────────────────────────────────────────────────────────┐
│ CLIENT (Browser)                                                 │
│ User A wants to update username                                  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ POST /api/users/set-username
                             │ Cookie: accessToken=USER_A_JWT
                             │ Body: { "username": "newname" }
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│ MIDDLEWARE: requireAuth                                          │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 1. Extract token from httpOnly cookie                       │ │
│ │    const token = request.cookies.accessToken                │ │
│ │                                                              │ │
│ │ 2. Verify JWT signature (cannot be forged)                  │ │
│ │    const decoded = verifyAccessToken(token)                 │ │
│ │    decoded = { userId: 1, email: "usera@test.com" }        │ │
│ │                                                              │ │
│ │ 3. Fetch user from database                                 │ │
│ │    const user = getUserById(decoded.userId)  // ID from JWT │ │
│ │                                                              │ │
│ │ 4. Attach authenticated user to request                     │ │
│ │    request.user = user  // { id: 1, username: "userA", ... }│ │
│ └─────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ request.user = { id: 1, ... }
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│ ROUTE HANDLER: /set-username                                     │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ ✅ SECURE: Get user ID from TOKEN (not from request body!)  │ │
│ │                                                              │ │
│ │ const userId = request.user.id  // 1 (User A's ID)         │ │
│ │ const { username } = request.body  // "newname"             │ │
│ │                                                              │ │
│ │ ❌ NEVER DO THIS:                                           │ │
│ │ const userId = request.body.userId  // Client can fake this!│ │
│ └─────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ userId=1, username="newname"
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│ SERVICE LAYER: userService.updateUsername()                      │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 1. Validate username format                                 │ │
│ │ 2. Check if username is available                           │ │
│ │ 3. Execute SQL update                                       │ │
│ │                                                              │ │
│ │    UPDATE users                                             │ │
│ │    SET username = 'newname'                                 │ │
│ │    WHERE id = 1 AND is_active = 1  ✅ ONLY updates User A  │ │
│ │                                                              │ │
│ │ 4. Return updated user object                               │ │
│ └─────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ { id: 1, username: "newname", ... }
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│ RESPONSE                                                         │
│ { success: true, user: { id: 1, username: "newname" } }        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🛡️ **Attack Prevention**

### **Attack Scenario: User A tries to modify User B**

```
┌─────────────────────────────────────────────────────────────────┐
│ MALICIOUS CLIENT                                                 │
│ User A (id=1) tries to update User B (id=2)                     │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ POST /api/users/set-username
                             │ Cookie: accessToken=USER_A_JWT (id=1)
                             │ Body: { "userId": 2, "username": "hacked" }
                             │                     ↑
                             │                     └─ Malicious: trying to update User B
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│ MIDDLEWARE: requireAuth                                          │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ JWT token contains User A's ID (id=1)                       │ │
│ │ request.user = { id: 1, username: "userA", ... }           │ │
│ └─────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ request.user.id = 1 (User A)
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│ ROUTE HANDLER: /set-username                                     │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ ✅ DEFENSE: Route IGNORES body.userId completely!           │ │
│ │                                                              │ │
│ │ const userId = request.user.id  // 1 (from token, not body!)│ │
│ │ const { username } = request.body  // "hacked"              │ │
│ │                                                              │ │
│ │ // body.userId (2) is IGNORED - never used!                │ │
│ └─────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ userId=1 (User A), username="hacked"
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│ SERVICE LAYER: userService.updateUsername(1, "hacked")          │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ UPDATE users                                                │ │
│ │ SET username = 'hacked'                                     │ │
│ │ WHERE id = 1  ✅ Updates User A (attacker's own account)   │ │
│ └─────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│ RESULT: ✅ ATTACK BLOCKED                                       │
│ - User A updated their OWN username to "hacked"                 │
│ - User B's data is UNTOUCHED                                    │
│ - Malicious userId in body was IGNORED                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## ✅ **Security Layers**

```
┌────────────────────────────────────────────────────────────┐
│ Layer 1: AUTHENTICATION                                     │
│ ✅ requireAuth middleware verifies JWT token                │
│ ✅ Token cannot be forged (signature verification)          │
│ ✅ Short expiry (15 minutes)                                │
└────────────────────────────────────────────────────────────┘
                             ↓
┌────────────────────────────────────────────────────────────┐
│ Layer 2: AUTHORIZATION                                      │
│ ✅ User ID from authenticated token (request.user.id)       │
│ ✅ Client cannot specify target user ID                     │
│ ✅ Principle of least privilege (own data only)             │
└────────────────────────────────────────────────────────────┘
                             ↓
┌────────────────────────────────────────────────────────────┐
│ Layer 3: DATA ACCESS CONTROL                                │
│ ✅ SQL WHERE clause limits update scope                     │
│ ✅ Parameterized queries (SQL injection protection)         │
│ ✅ is_active check (soft delete protection)                 │
└────────────────────────────────────────────────────────────┘
                             ↓
┌────────────────────────────────────────────────────────────┐
│ Layer 4: TRANSPORT SECURITY                                 │
│ ✅ httpOnly cookies (XSS protection)                        │
│ ✅ SameSite: strict (CSRF protection)                       │
│ ✅ HTTPS in production (MITM protection)                    │
└────────────────────────────────────────────────────────────┘
```

---

## 🎯 **Key Security Principle**

### **✅ CORRECT Pattern**

```javascript
fastify.post('/update-profile', {
  preHandler: requireAuth  // ✅ Authenticate first
}, async (request, reply) => {
  // ✅ ALWAYS: Get user ID from authenticated token
  const userId = request.user.id
  
  // ✅ ONLY accept the data to update (not the target user ID)
  const { newData } = request.body
  
  // ✅ Update only authenticated user's data
  const result = await userService.updateData(userId, newData)
  
  return { success: true, user: result }
})
```

### **❌ INSECURE Pattern (DON'T DO THIS)**

```javascript
fastify.post('/update-profile', async (request, reply) => {
  // ❌ WRONG: User ID from request body (client can fake this!)
  const { userId, newData } = request.body
  
  // ❌ DANGER: User A can update User B's data!
  const result = await userService.updateData(userId, newData)
  
  return { success: true, user: result }
})
```

---

## 📋 **Quick Checklist**

When implementing user update routes:

- [ ] ✅ Use `preHandler: requireAuth` to authenticate
- [ ] ✅ Get user ID from `request.user.id` (not body/params)
- [ ] ✅ Accept only the update data in request body
- [ ] ✅ Service method uses authenticated user ID
- [ ] ✅ SQL WHERE clause includes user ID
- [ ] ❌ NEVER accept target user ID from client
- [ ] ❌ NEVER trust client for authorization decisions

---

## 🎉 **Conclusion**

**Current implementation is SECURE because:**

1. User ID comes from authenticated JWT token
2. Route uses `request.user.id` (from token, not body)
3. Client cannot specify which user to update
4. Multiple security layers provide defense in depth

**User A CANNOT modify User B's information!** ✅

---

**See detailed analysis:** `/docs/backend/AUTHORIZATION_SECURITY_ANALYSIS.md`
