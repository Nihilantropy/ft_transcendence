# Production Environment Configuration
# ⚠️  CRITICAL: Change JWT_SECRET_KEY before deploying to production!
# Generate a secure key with: openssl rand -hex 32

# Server
PORT=8001
HOST=0.0.0.0
DEBUG=false
LOG_LEVEL=info

# JWT Configuration
# ⚠️  CHANGE THIS IN PRODUCTION! Must match Auth Service secret key
JWT_SECRET_KEY=CHANGE-THIS-TO-A-SECURE-RANDOM-SECRET-KEY-IN-PRODUCTION
JWT_ALGORITHM=HS256

# Backend Services
# These should match the service names in docker-compose.yml
AUTH_SERVICE_URL=http://auth-service:3001
USER_SERVICE_URL=http://user-service:3002
AI_SERVICE_URL=http://ai-service:3003

# Redis
# Use the Redis service from docker-compose or external Redis URL
REDIS_URL=redis://redis:6379/0

# Rate Limiting
# Adjust based on expected traffic and abuse prevention needs
RATE_LIMIT_PER_MINUTE=60

# Production Deployment Notes:
# 1. Generate secure JWT_SECRET_KEY: openssl rand -hex 32
# 2. Ensure JWT_SECRET_KEY matches between Auth Service and API Gateway
# 3. Update backend service URLs if using external services
# 4. Configure Redis with password in production: redis://:password@redis:6379/0
# 5. Adjust RATE_LIMIT_PER_MINUTE based on load testing
# 6. Set up log aggregation (ELK, CloudWatch, etc.) for structured logs
# 7. Enable HTTPS/TLS in production environment
# 8. Configure firewall rules to allow only necessary ports
